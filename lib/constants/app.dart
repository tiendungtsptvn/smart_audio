/// Key for auth Service
const String spotifyClientId = 'your_key';
const String spotifyClientSecret = 'your_key';

/// Position to decide when a track is stored (for recommendation) to storage.
const double positionToStoreTrack = 0.8;